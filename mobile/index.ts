console.log("[App] Starting initialization...");

// Note: Download notification handlers are now registered in app/_layout.tsx
// This ensures they're set up after the app properly initializes

// Initialize router
import "expo-router/entry";
